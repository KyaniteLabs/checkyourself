# ROLE: LUNA FIX TEAM — RE-VOTE REMEDIATION (checkyourself, final pass)
The sol re-vote found: (a) W9/W10 register scopes not verifiably implemented — implement them NOW per MASTER-REGISTER rows; (b) retrofit artifacts inside the repo make python3 tools/validate_public.py RED (2 broken links in _retrofit-2026-09-04/FINDINGS-*.md) — apply the SAME cure tastecheck used: remove/de-markdownize link-like text in those FINDINGS files so the public validator passes; (c) partial rows MR-033 (document dashboard inline in 05_OUTPUT_TEMPLATES/README.md + 10_DASHBOARD/README.md), MR-034 (SECURITY.md tagged-release line), MR-038 (manual rule-ID registry/rubric), MR-039 (base-score/minimum-cap formula + executable-contract link) — close each per register scope.
Acceptance: python3 tools/validate_public.py GREEN + python3 -m pytest tests/ -q fully green. Record outputs.
GIT: no git commands. Write _retrofit-2026-09-04/REMED-REPORT.md (per-item status + evidence + changed-file list). No installs, no network.
