# Cursor / Windsurf Adapter

Start with [`native-cli-mcp.md`](native-cli-mcp.md); this file adds editor
context steps.

1. Put `checkyourself/` inside the repo and open the chat/agent panel.
2. Ask:

```text
Use the checkyourself folder as context. Start with read-only diagnostic mode. Do not edit files until I approve a specific fix. Generate the learning plan after the report.
```

3. For approval, ask for a small diff and verification.
